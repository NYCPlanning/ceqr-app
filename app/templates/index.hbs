<div class="ui column grid container site">
  <div class="row">
    <ul>
      {{#each model as |project|}}
        <li>{{#link-to 'project' project.id}}{{project.id}}{{/link-to}}</li>
      {{/each}}
    </ul>
  </div>

  <div class="row">
    <button class="ui green button" {{action "newProject"}}>
      New Project
    </button>
  </div>
</div>
