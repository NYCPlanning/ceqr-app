<table class="ui attached celled table">
  <thead>
    <tr>
      <th class="three wide">Study Area</th>
      <th class="two wide">Projected {{project.buildYear}} Enrollment</th>
      <th class="two wide">Students Introduced by No-Action Residential Development</th>
      <th class="two wide">Total No-Action Enrollment</th>
      <th class="one wide">Capacity</th>
      <th class="one wide">Available Seats</th>
      <th class="one wide">Utilization (%)</th>
    </tr>
  </thead>

  {{#if psTable}}
    <thead>
      <tr>
        <th colspan=7>Primary Schools</th>
      </tr>
    </thead>

    <tbody>
      {{#each psTable as |p|}}
        <tr>
          <td>District {{p.district}} - Subdistrict {{p.subdistrict}}</td>
          <td>{{p.enroll}}</td>
          <td>{{p.students}}</td>
          <td>{{p.enrollTotal}}</td>
          <td>{{p.capacityTotal}}</td>
          <td>{{p.seats}}</td>
          <td>{{p.utilization}}</td>
        </tr>
      {{/each}}
    </tbody>
  {{/if}}

  {{#if isTable}}
    <thead>
      <tr>
        <th colspan=7>Intermediate Schools</th>
      </tr>
    </thead>

    <tbody>
      {{#each isTable as |p|}}
        <tr>
          <td>District {{p.district}} - Subdistrict {{p.subdistrict}}</td>
          <td>{{p.enroll}}</td>
          <td>{{p.students}}</td>
          <td>{{p.enrollTotal}}</td>
          <td>{{p.capacityTotal}}</td>
          <td>{{p.seats}}</td>
          <td>{{p.utilization}}</td>
        </tr>
      {{/each}}
    </tbody>
  {{/if}}

  {{#if hsTable}}
    <thead>
      <tr>
        <th colspan=7>High Schools</th>
      </tr>
    </thead>

    <tbody>
      {{#each hsTable as |p|}}
        <tr>
          <td>District {{p.district}} - Subdistrict {{p.subdistrict}}</td>
          <td>{{p.enroll}}</td>
          <td>{{p.students}}</td>
          <td>{{p.enrollTotal}}</td>
          <td>{{p.capacityTotal}}</td>
          <td>{{p.seats}}</td>
          <td>{{p.utilization}}</td>
        </tr>
      {{/each}}
    </tbody>
  {{/if}}
</table>
