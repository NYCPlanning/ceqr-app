/* eslint-env node */
'use strict';

module.exports = {
  extends: 'recommended',

  rules: {
    // Should probably apply these eventually
    'no-invalid-interactive': warn,
    'no-inline-styles': warn
  }
};
