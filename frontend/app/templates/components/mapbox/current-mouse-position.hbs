{{! binds a map event with an action using mapbox-gl's on method }}
<MapboxGlOn
  @event="mousemove"
  @action={{action "handleMouseMove"}}
  @eventSource={{this.map.instance}}
 />

<MapboxGlOn
  @event="mouseout"
  @action={{action "handleMouseLeave"}}
  @eventSource={{this.map.instance}}
 />

{{yield this.mousePosition}}