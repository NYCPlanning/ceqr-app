<div class="ui {{if analysisNeeded "yellow" "grey"}} segment">
  <h3>
    {{yield}}
    <div class="ui big horizontal label {{if analysisNeeded "yellow" "grey"}}">
      {{if analysisNeeded "Yes" "No"}}
    </div>
  </h3>
</div>