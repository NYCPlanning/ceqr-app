import Route from '@ember/routing/route';

export default class ProjectShowTransportationTdfPlanningFactorsIndexRoute extends Route {
  // redirect(model) {
  //   console.log(model);
  //   let project = { id: model.project.id };
  //   let factor  = { transportation_planning_factor_id: model.transportationPlanningFactors.firstObject.id };
  //   this.transitionTo('project.show.transportation.tdf.planning-factors.show', project, factor);
  // }
}
